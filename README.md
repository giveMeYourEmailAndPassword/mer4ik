# 🎨 Мерч-конструктор

Мобильный веб-сервис для создания собственного мерча с загрузкой логотипов и фото.

## 🚀 Быстрый старт

### Требования
- Node.js 16+
- npm или yarn

### Установка и запуск

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd mer4ik

# Удалите старые зависимости и node_modules если есть
rm -rf node_modules package-lock.json

# Установите зависимости
npm install

# Запустите dev сервер
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

## 📱 Функционал

### ✨ Основные возможности:
- **3 типа товаров**: футболка, кружка, худи
- **Загрузка изображений**: поддержка PNG, JPG, SVG
- **Редактирование**: перемещение, масштабирование, вращение, прозрачность
- **Тач-жесты**: полная поддержка для мобильных устройств
- **Экспорт**: сохранение в PNG/JPEG высоком качестве

### 🎯 Mobile UX:
- **Адаптивный дизайн** с мобильным-first подходом
- **Большие кнопки** для удобного использования пальцами
- **Плавные анимации** с hardware acceleration
- **Поддержка жестов**: pinch-to-zoom, rotate, drag
- **Отключение pull-to-refresh** и системного zoom

## 🛠️ Технологии

- **React 18** + TypeScript
- **Vite** для быстрой сборки
- **TailwindCSS** для стилизации
- **React-Konva** для работы с canvas
- **Lucide React** для иконок

## 📁 Структура проекта

```
src/
├── components/          # React компоненты
│   ├── ui/             # Базовые UI компоненты
│   ├── TemplateSelector.tsx
│   ├── MerchEditor.tsx
│   └── ImageControls.tsx
├── hooks/              # Custom hooks
│   ├── useTouchGestures.ts
│   └── useImageExport.ts
├── types/              # TypeScript типы
└── utils/              # Утилиты
```

## 🎮 Как использовать

1. **Выберите товар** на главном экране
2. **Загрузите изображение** через кнопку "Загрузить изображение"
3. **Отредактируйте дизайн**:
   - Перемещайте изображение пальцем/мышью
   - Масштабируйте pinch-to-zoom или кнопками +/-
   - Вращайте swipe или кнопками поворота
   - Настраивайте прозрачность слайдером
4. **Скачайте результат** в формате PNG или JPEG

## 🔧 Оптимизация

- **Кэширование изображений** для быстрой работы
- **Оптимизация canvas** с правильным pixelRatio
- **Lazy loading** компонентов
- **Минификация** бандла
- **Tree shaking** неиспользуемых зависимостей

## 🐛 Troubleshooting

### Ошибка "Cannot read properties of undefined (reading 'ReactCurrentOwner')"

Эта ошибка возникает из-за конфликта версий React или множественных экземпляров React.

**Способ 1: Быстрая чистка**
```bash
# Очистите кэш и переустановите зависимости
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
npm run dev
```

**Способ 2: Использование скрипта**
```bash
# Сделайте скрипт исполняемым и запустите
chmod +x clean-install.sh
./clean-install.sh
```

**Способ 3: Если проблема продолжается**
Проект автоматически переключится на fallback-редактор на базе Canvas API, который предоставляет все основные функции:
- ✅ Загрузка изображений
- ✅ Масштабирование и перемещение
- ✅ Вращение
- ✅ Экспорт в PNG
- ✅ Полная мобильная поддержка

### Другие проблемы

**Медленная работа на мобильных:**
- Используйте `pixelRatio: 1` для отображения
- Оптимизируйте размер загружаемых изображений

**Проблемы с тач-жестами:**
- Убедитесь, что мета-тег viewport настроен правильно
- Проверьте CSS свойство `touch-action`

### Медленная работа на мобильных

**Решения:**
- Используйте `pixelRatio: 1` для отображения, `pixelRatio: 2` только для экспорта
- Оптимизируйте размер загружаемых изображений
- Включите hardware acceleration в CSS

### Проблемы с тач-жестами

**Проверьте:**
- Мета-тег viewport в index.html
- CSS свойство `touch-action`
- Отключите системный zoom

## 🔮 TODO

- [ ] Добавить больше шаблонов
- [ ] WebRTC камера для фото
- [ ] PWA поддержка
- [ ] Облачное хранилище
- [ ] Социальные функции
- [ ] AR предпросмотр

## 📄 Лицензия

MIT License

---

**Создано с ❤️ для мобильных устройств**
